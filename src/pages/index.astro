---
import BaseLayout from '../layouts/BaseLayout.astro';
import siteData from '../data/site.json';
import OptimizedImage from '../components/OptimizedImage.astro';
---

<BaseLayout title="Rayla Seraphine">
  <div class="home">
    <div class="left">
      <div class="content">
        <h1>Rayla Seraphine Dashyy</h1>
        <p class="muted">Hey there,</p>
        <p class="muted">my name is Rayla Seraphine and I'll just put some pictures on here.</p>
        <p class="muted">All self-made of course c:</p>
        <a href="/collection" class="cta-button">Pictures</a>
        <h2>Changelog:</h2>
        <ul class="changelog">
          {siteData.changelog.map((item) => (
            <li>{item.title} - {item.date}</li>
          ))}
        </ul>
      </div>
    </div>
    <div class="right">
      <OptimizedImage src={siteData.hero_image} alt="" width={1600} loading="eager" />
    </div>
  </div>
</BaseLayout>

<style>
  .home {
    display: grid;
    grid-template-columns: 1fr 1fr;
    min-height: calc(100vh - 64px);
  }

  .left {
    display: flex;
    align-items: center;
    justify-content: center;
    padding: var(--space-lg);
  }

  .content {
    max-width: 480px;
    text-align: center;
  }

  .content h1 {
    font-size: clamp(36px, 4.5vw, 64px);
    font-weight: 800;
    letter-spacing: -1.5px;
    line-height: 1.05;
    margin-bottom: var(--space-md);
    text-align: left;
  }

  .muted {
    color: var(--text-muted);
    font-size: 14px;
    margin-top: var(--space-xs);
  }

  .cta-button {
    display: inline-block;
    background: #e5e0d5;
    color: #000;
    padding: 12px 32px;
    border-radius: 999px;
    font-size: 14px;
    font-weight: 500;
    margin: var(--space-lg) 0;
    transition: background 0.15s ease;
  }

  .cta-button:hover {
    background: #fff;
  }

  .content h2 {
    font-size: 18px;
    font-weight: 700;
    margin-top: var(--space-md);
    margin-bottom: var(--space-sm);
  }

  .changelog {
    list-style: none;
    padding: 0;
  }

  .changelog li {
    color: var(--text-muted);
    font-size: 13px;
    margin-bottom: 6px;
  }

  .right {
    position: relative;
    overflow: hidden;
    background: #000;
    min-height: calc(100vh - 64px);
  }

  .right img {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  @media (max-width: 720px) {
    .home {
      grid-template-columns: 1fr;
    }
    .right {
      height: 50vh;
      min-height: 0;
      order: -1;
    }
  }
</style>